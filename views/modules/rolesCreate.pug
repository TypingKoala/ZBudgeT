.masonry-item.col-md-6
    .bgc-white.p-20.bd
        h6.c-grey-900 Create a New Role
        .mT-30
            form(method="post" action="/roles/create")
                .form-group
                label(for='roleName') Role Name
                input.form-control#password(type='text', name='roleName' placeholder="superadmin")
                .form-group
                label(for='permissions') Role Permissions
                input.form-control#newPassword(type='text', name='permissions' placeholder="admin, budget manager")
                .form-group
                button.btn.btn-primary(type='submit') Submit